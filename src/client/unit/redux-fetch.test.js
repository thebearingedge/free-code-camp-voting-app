
import { expect, stub } from '@thebearingedge/test-utils'
import { createStore, applyMiddleware } from 'redux'
import reduxFetch from '../redux-fetch'


describe('redux-fetch', () => {

  it('runs tests!', () => expect(reduxFetch).to.be.a('function'))

})
